<Window x:Class="Synkro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Synkro"
        Title="Synkro"
        Width="360" Height="540"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="#1E1E2E"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:StringToVisibilityConverter x:Key="StringToVis"/>

        <!-- Color palette -->
        <Color x:Key="BgColor">#1E1E2E</Color>
        <Color x:Key="SurfaceColor">#2A2A3C</Color>
        <Color x:Key="AccentColor">#7C3AED</Color>
        <Color x:Key="AccentHoverColor">#9061F9</Color>
        <Color x:Key="TextColor">#E4E4EF</Color>
        <Color x:Key="SubtextColor">#8888A0</Color>
        <Color x:Key="BorderColor">#3A3A50</Color>
        <Color x:Key="WarningColor">#F59E0B</Color>
        <Color x:Key="ErrorColor">#EF4444</Color>
        <Color x:Key="SuccessColor">#10B981</Color>

        <SolidColorBrush x:Key="BgBrush" Color="{StaticResource BgColor}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="SubtextBrush" Color="{StaticResource SubtextColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>

        <!-- Modern rounded button -->
        <Style x:Key="RoundBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource BorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary accent button -->
        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource RoundBtn}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern slider style -->
        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Height="4" CornerRadius="2"
                                    Background="{StaticResource BorderBrush}"
                                    VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="16" Height="16"
                                                         Fill="{StaticResource AccentBrush}"
                                                         Cursor="Hand"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Checkbox style -->
        <Style x:Key="ModernCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="box" Width="18" Height="18"
                                    CornerRadius="4"
                                    BorderThickness="2"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    Background="Transparent"
                                    VerticalAlignment="Center">
                                <TextBlock x:Name="check" Text="&#x2713;"
                                           FontSize="12" FontWeight="Bold"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="box" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Hidden scrollbar - scroll with mouse wheel only -->
        <Style TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <!-- Channel mode toggle (S/L/R) -->
        <Style x:Key="ChannelToggle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource SubtextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Width="22" Height="20"
                                CornerRadius="4"
                                Background="{StaticResource BgBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox -->
        <ControlTemplate x:Key="ComboBoxToggle" TargetType="ToggleButton">
            <Border x:Name="bd" Background="{StaticResource BgBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    CornerRadius="6" Padding="8,4">
                <Grid>
                    <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"
                                      Margin="0,0,18,0"/>
                    <Path x:Name="arrow" Data="M0,0 L4,4 L8,0" Stroke="{StaticResource SubtextBrush}"
                          StrokeThickness="1.5" HorizontalAlignment="Right"
                          VerticalAlignment="Center" Margin="0,1,2,0"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="DarkCombo" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Template="{StaticResource ComboBoxToggle}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False" ClickMode="Press"/>
                            <ContentPresenter IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="10,5,24,5"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" Focusable="False"
                                   AllowsTransparency="True" PopupAnimation="Fade">
                                <Border Background="{StaticResource SurfaceBrush}"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1" CornerRadius="6"
                                        Padding="2" Margin="0,2,0,0"
                                        MaxHeight="200">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="{x:Type ComboBoxItem}" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource BorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close button (macOS-style red dot) -->
        <Style x:Key="CloseBtn" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Width="14" Height="14">
                            <Ellipse x:Name="dot" Fill="#FF5F57"/>
                            <TextBlock x:Name="x" Text="&#x2715;" FontSize="8"
                                       Foreground="#80000000"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="dot" Property="Fill" Value="#FF3B30"/>
                                <Setter TargetName="x" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="dot" Property="Fill" Value="#CC2F26"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Remove slot button -->
        <Style x:Key="RemoveSlotBtn" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="Transparent" CornerRadius="4">
                            <TextBlock Text="&#x2715;" FontSize="10"
                                       Foreground="{StaticResource SubtextBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#40EF4444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#80EF4444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main container (sharp edges, no transparency artifacts) -->
    <Border x:Name="MainBorder"
            Background="{StaticResource BgBrush}"
            BorderBrush="#505068" BorderThickness="1"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Padding="16,14,16,10"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <DockPanel>
                    <Button Content="&#x2715;" DockPanel.Dock="Right"
                            Style="{StaticResource CloseBtn}"
                            FontSize="12"
                            Click="Close_Click"/>
                    <StackPanel>
                        <TextBlock Text="Synkro"
                                   Foreground="{StaticResource TextBrush}"
                                   FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Status}"
                                   Foreground="{StaticResource SuccessBrush}"
                                   FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="16,12,16,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: Source selector -->
                <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                        CornerRadius="8" Padding="12,8" Margin="0,0,0,10">
                    <DockPanel>
                        <TextBlock Text="Source"
                                   Foreground="{StaticResource SubtextBrush}"
                                   FontSize="11" VerticalAlignment="Center"
                                   DockPanel.Dock="Left" Margin="0,0,8,0"/>
                        <ComboBox ItemsSource="{Binding CaptureDevices}"
                                  SelectedItem="{Binding SelectedCaptureDevice}"
                                  Style="{StaticResource DarkCombo}"/>
                    </DockPanel>
                </Border>

                <!-- Row 1: Output device list -->
                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Hidden"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding OutputSlots}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource SurfaceBrush}"
                                        CornerRadius="8"
                                        Padding="14,10"
                                        Margin="0,0,0,8">
                                    <StackPanel>
                                        <!-- Device selector + remove button -->
                                        <DockPanel>
                                            <Button DockPanel.Dock="Right"
                                                    Style="{StaticResource RemoveSlotBtn}"
                                                    Click="RemoveDevice_Click"
                                                    Tag="{Binding}"
                                                    Margin="6,0,0,0"/>
                                            <ComboBox ItemsSource="{Binding AvailableDevices}"
                                                      DisplayMemberPath="FriendlyName"
                                                      SelectedValuePath="Id"
                                                      SelectedValue="{Binding SelectedDeviceId}"
                                                      Style="{StaticResource DarkCombo}"/>
                                        </DockPanel>

                                        <!-- Error message -->
                                        <TextBlock Text="{Binding ErrorMessage}"
                                                   Foreground="{StaticResource ErrorBrush}"
                                                   FontSize="10" Margin="0,4,0,0"
                                                   TextWrapping="Wrap"
                                                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVis}}"/>

                                        <!-- Volume control -->
                                        <DockPanel Margin="0,8,0,0">
                                            <TextBlock Text="Vol"
                                                       Foreground="{StaticResource SubtextBrush}"
                                                       FontSize="10" Width="24"
                                                       VerticalAlignment="Center"/>
                                            <Button Content="-" Width="24" Height="24"
                                                    Style="{StaticResource RoundBtn}"
                                                    Click="VolDown_Click" Tag="{Binding}"
                                                    DockPanel.Dock="Left" Margin="0,0,4,0"
                                                    FontSize="14" FontWeight="Bold"/>
                                            <TextBlock DockPanel.Dock="Right"
                                                       Text="{Binding Volume, StringFormat='{}{0:P0}'}"
                                                       Foreground="{StaticResource TextBrush}"
                                                       Width="42" TextAlignment="Right"
                                                       FontSize="11" VerticalAlignment="Center"/>
                                            <Button Content="+" Width="24" Height="24"
                                                    Style="{StaticResource RoundBtn}"
                                                    Click="VolUp_Click" Tag="{Binding}"
                                                    DockPanel.Dock="Right" Margin="4,0,4,0"
                                                    FontSize="14" FontWeight="Bold"/>
                                            <Slider Minimum="0" Maximum="5"
                                                    Value="{Binding Volume}"
                                                    Style="{StaticResource ModernSlider}"
                                                    TickFrequency="0.01"
                                                    IsSnapToTickEnabled="True"
                                                    VerticalAlignment="Center"
                                                    Margin="4,0"/>
                                        </DockPanel>

                                        <!-- Channel mode + Fine-tune + Delay -->
                                        <DockPanel Margin="0,6,0,0">
                                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                                <RadioButton Content="S" GroupName="{Binding DeviceId}"
                                                             IsChecked="{Binding IsStereo}"
                                                             Style="{StaticResource ChannelToggle}"/>
                                                <RadioButton Content="L" GroupName="{Binding DeviceId}"
                                                             IsChecked="{Binding IsLeft}"
                                                             Style="{StaticResource ChannelToggle}"
                                                             Margin="3,0,0,0"/>
                                                <RadioButton Content="R" GroupName="{Binding DeviceId}"
                                                             IsChecked="{Binding IsRight}"
                                                             Style="{StaticResource ChannelToggle}"
                                                             Margin="3,0,0,0"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right"
                                                        VerticalAlignment="Center">
                                                <TextBlock Text="Tune"
                                                           Foreground="{StaticResource SubtextBrush}"
                                                           FontSize="10" VerticalAlignment="Center"
                                                           Margin="8,0,4,0"/>
                                                <Button Content="-5" Width="26" Height="20"
                                                        Style="{StaticResource RoundBtn}"
                                                        Click="FineTuneDown_Click" Tag="{Binding}"
                                                        FontSize="9"/>
                                                <TextBlock Text="{Binding FineTuneMs, StringFormat='{}{0}ms'}"
                                                           Foreground="{StaticResource AccentBrush}"
                                                           FontSize="10" FontWeight="SemiBold"
                                                           Width="36" TextAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                                <Button Content="+5" Width="26" Height="20"
                                                        Style="{StaticResource RoundBtn}"
                                                        Click="FineTuneUp_Click" Tag="{Binding}"
                                                        FontSize="9"/>
                                            </StackPanel>
                                        </DockPanel>

                                        <!-- Delay display -->
                                        <TextBlock Foreground="{StaticResource SubtextBrush}"
                                                   FontSize="10" Margin="0,4,0,0">
                                            <Run Text="delay "/>
                                            <Run Text="{Binding DelayMs, Mode=OneWay}"/>
                                            <Run Text="ms"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Row 2: Add Device button -->
                <Button Grid.Row="2"
                        Content="+ Add Output Device"
                        Command="{Binding AddDeviceCommand}"
                        Style="{StaticResource RoundBtn}"
                        Height="32" FontSize="12"
                        Margin="0,4,0,10"/>

                <!-- Row 3: Start/Stop button -->
                <Button Grid.Row="3"
                        Content="{Binding StartStopText}"
                        Command="{Binding StartStopCommand}"
                        Style="{StaticResource PrimaryBtn}"
                        Height="42" FontSize="15"
                        Foreground="White"/>
            </Grid>
        </Grid>
    </Border>
</Window>
